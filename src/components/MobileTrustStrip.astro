---
import { property } from '../data/property';
import { Star, Wifi, Snowflake, Users, MapPin } from 'lucide-astro';
---

<div class="mobile-trust-strip" aria-label="Key property assurances">
  <div class="container">
    <div class="trust-track">
      <div class="trust-badge">
        <div class="stars-icon">
          <Star size={13} fill="#dcb567" stroke="#dcb567" />
          <Star size={13} fill="#dcb567" stroke="#dcb567" />
          <Star size={13} fill="#dcb567" stroke="#dcb567" />
          <Star size={13} fill="#dcb567" stroke="#dcb567" />
          <Star size={13} fill="#dcb567" stroke="#dcb567" />
        </div>
        <span><strong>{property.googleRatingDisplay}</strong> Google Rating</span>
      </div>

      <div class="trust-divider"></div>

      <div class="trust-badge">
        <Wifi size={14} class="badge-icon" />
        <span>Free Wi‑Fi</span>
      </div>

      <div class="trust-divider"></div>

      <div class="trust-badge">
        <Snowflake size={14} class="badge-icon" />
        <span>Full AC Rooms</span>
      </div>

      <div class="trust-divider"></div>

      <div class="trust-badge">
        <Users size={14} class="badge-icon" />
        <span>Family Suites</span>
      </div>

      <div class="trust-divider"></div>

      <div class="trust-badge">
        <MapPin size={14} class="badge-icon" />
        <span>Prime Varanasi Access</span>
      </div>
    </div>
  </div>
</div>

<style>
.mobile-trust-strip {
  background: #fbf3e6;
  border-bottom: 1px solid rgba(185, 137, 63, 0.2);
  padding: 12px 0;
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none;
}

.mobile-trust-strip::-webkit-scrollbar {
  display: none;
}

.trust-track {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 16px;
  width: max-content;
  margin: 0 auto;
  padding: 0 16px;
}

.trust-badge {
  display: flex;
  align-items: center;
  gap: 7px;
  font-size: 11.5px;
  font-weight: 600;
  color: #55342a;
  white-space: nowrap;
  padding: 4px 8px;
  border-radius: 99px;
  transition: all 0.35s cubic-bezier(0.2, 0.8, 0.2, 1);
  cursor: default;
}

.trust-badge:hover {
  background: rgba(185, 137, 63, 0.12);
  color: var(--wine);
}

.trust-badge strong {
  color: var(--wine);
}

.stars-icon {
  display: flex;
  gap: 2px;
  transition: transform 0.3s ease, filter 0.3s ease;
}

.trust-badge:hover .stars-icon {
  transform: scale(1.15);
  filter: drop-shadow(0 0 6px rgba(220, 181, 103, 0.8));
}

:global(.badge-icon) {
  color: #b9893f;
  flex-shrink: 0;
  transition: color 0.35s ease, transform 0.35s ease, filter 0.35s ease;
}

.trust-badge:hover :global(.badge-icon) {
  color: #8c581a;
  transform: translateY(-2px) scale(1.2);
  filter: drop-shadow(0 0 6px rgba(220, 181, 103, 0.7));
}

.trust-divider {
  width: 1px;
  height: 14px;
  background: rgba(185, 137, 63, 0.3);
  flex-shrink: 0;
}

@media (max-width: 768px) {
  .trust-track {
    justify-content: flex-start;
  }
  .trust-badge:active {
    transform: scale(0.96);
    background: rgba(185, 137, 63, 0.15);
  }
}
</style>
